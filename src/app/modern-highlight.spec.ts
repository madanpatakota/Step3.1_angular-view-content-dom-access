import { ModernHighlight } from './modern-highlight';

describe('ModernHighlight', () => {
  it('should create an instance', () => {
    const directive = new ModernHighlight();
    expect(directive).toBeTruthy();
  });
});
