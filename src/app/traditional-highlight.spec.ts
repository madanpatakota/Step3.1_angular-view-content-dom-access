import { TraditionalHighlight } from './traditional-highlight';

describe('TraditionalHighlight', () => {
  it('should create an instance', () => {
    const directive = new TraditionalHighlight();
    expect(directive).toBeTruthy();
  });
});
